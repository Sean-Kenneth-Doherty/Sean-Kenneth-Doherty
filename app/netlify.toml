[build]
  publish = "dist"
  command = "npm run build"

# Handle client-side routing for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Enable Netlify Forms
[dev]
  command = "npm run dev"
  port = 5173
  publish = "dist"
